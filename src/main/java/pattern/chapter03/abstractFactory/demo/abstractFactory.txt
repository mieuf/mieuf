抽象工厂模式(Abstract Factory Pattern)
   Provide an interface for creating families of related or dependent object without specifying
   their concrete classes.
   (为创建一组相关或者相互依赖的对象提供一个接口，而且无须指定他们的具体类)
 特点:封装性，内部约束私有，扩展困难